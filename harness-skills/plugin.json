{
  "name": "agentcore-harness",
  "version": "0.1.0",
  "description": "Default roles on AgentCore Runtime: Claude Code = backend builder, Codex = frontend builder on Bedrock Runtime Responses, Kiro = validator (authors an executable check; the engine runs it and reads its real exit code). The request determines the deliverable. Skills cover per-agent setup/deploy, the GitHub Gateway, the harness lifecycle, and restoring alternate roles with WORKSHOP_ROLES.",
  "author": {
    "name": "AgentCore Coding Agents Workshop"
  },
  "license": "MIT-0"
}
